# Servidor
PORT=3000
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173

# Auth (placeholder, luego lo cambiamos)
JWT_SECRET=dev_jwt_secret_change_me
JWT_EXPIRES_IN=900s
REFRESH_SECRET=dev_refresh_secret_change_me
REFRESH_EXPIRES_IN=7d

# Webhook (para n8n, más adelante)
WEBHOOK_MACHINE_TOKEN=change_me

# Prisma/DB (lo configuramos en el próximo bloque)
# DATABASE_URL=postgresql://user:pass@localhost:5432/contable?schema=public

# S3/MinIO (lo configuramos cuando lleguemos a archivos)
# S3_ENDPOINT=http://localhost:9000
# S3_BUCKET=invoices
# S3_ACCESS_KEY=minioadmin
# S3_SECRET_KEY=minioadmin
# S3_USE_SSL=false
