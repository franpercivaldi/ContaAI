{
  "name": "contable-monorepo",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm --filter @apps/api dev",
    "lint": "eslint .",
    "typecheck": "tsc -b --pretty false"
  },
  "devDependencies": {
    "eslint": "^9.13.0",
    "typescript": "^5.6.3"
  }
}
